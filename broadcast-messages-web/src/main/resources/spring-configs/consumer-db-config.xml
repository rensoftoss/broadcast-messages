<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
	   http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <!--Persist message into database-->

    <int:channel id="queueToDatabaseChannel">
        <int:dispatcher task-executor="dbChannelExecutor"/>
    </int:channel>

    <task:executor id="dbChannelExecutor" pool-size="0-10" keep-alive="120" queue-capacity="500"/>

    <int:outbound-channel-adapter channel="queueToDatabaseChannel"
                                  ref="persistMessage" method="processMessage"/>

    <bean id="persistMessage" class="com.rensoftoss.broadcastmessages.PersistMessage"/>

</beans>